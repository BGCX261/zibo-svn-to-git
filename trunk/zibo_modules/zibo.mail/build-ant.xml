<?xml version="1.0" encoding="UTF-8"?>
<project name="zibo.mail" default="packageMail">

    <property name="phing.command" value="phing" />

	<target name="packageMail">
        <exec dir="" executable="${phing.command}">
            <arg line="packageMail" />
        </exec>
    </target>

</project>